# CÔNG TẮC TIẾP QUẢN DỰ ÁN (AI CONTEXT SWITCH)

**Mục đích:** File này dùng để khôi phục "trí nhớ" cho AI khi bạn chuyển sang máy tính khác làm việc hoặc reset máy.
**Lệnh gọi AI trên máy tính mới:** *"Bạn hãy đọc file `Lichsu[...]` trong thư mục Supra Phú Thọ để tiếp tục"*

---

## 1. Môi trường làm việc (Workspace)
- **Thư mục dự án chính xác:** `g:\My Drive\Training AI\Supra Phú Thọ`
- **Lưu ý cho AI:** Cần chú ý kỹ thư mục làm việc, không được nhầm lẫn với project `LogisticsHub` (vì 2 thư mục có chung nhiều file giống nhau như `trips_logic2.js`).

## 2. Các quy ước cốt lõi (Bắt buộc AI phải tuân theo)
- **Nguồn Logic Chia Tuyến (QUAN TRỌNG NHẤT):**
  - Toàn bộ luật chia tuyến, gom hàng, phân loại xe hiện đã được xuất ra định dạng Word.
  - AI **bắt buộc** chỉ được tham chiếu và đọc logic từ file: `Logic_Chia_Tuyen.docx` (nằm trong thư mục Supra Phú Thọ). 
  - Người dùng sẽ cập nhật trực tiếp trên file Word đó, AI tuyệt đối không tự biên diễn hoặc lấy logic từ các file js cũ nếu có sự khác biệt.
- **Nguồn Chia Cửa Hàng (GXT vs Giao Thẳng):**
  - Các script và thuật toán **chỉ sử dụng file `ChiaCuaHang.xlsx`** làm căn cứ để xác định một cửa hàng thuộc cụm "GXT" hay "Giao thẳng", không sử dụng lại file tổng `Danh sách Winmart (1).xlsx`.

## 3. Tiến độ đã hoàn thành
### Giai đoạn trước (Tính đến 20/06/2026)
- **Chuẩn hóa dữ liệu Winmart:** Đã xử lý 4920 cửa hàng đầu tiên + 1672 cửa hàng mới. Dữ liệu chuẩn tại `DS_Winmart_Extracted.xlsx`.
- **Phân luồng định tuyến:** Đã tạo file `ChiaCuaHang.xlsx` (152 siêu thị GXT và phần còn lại là Giao thẳng).
- **Tài liệu:** Đã cập nhật file `Logic_Chia_Tuyen.docx` Phần 2.

### Giai đoạn hiện tại (Tính đến 02h41 ngày 23/06/2026)
- **Cập nhật Giao diện (UI) (`demo.html`):** 
  - Đã đổi tên toàn hệ thống và logo từ "Logistics Hub" thành **"Supra Phú Thọ"** (Ký hiệu: SP).
  - Đã ẩn và gỡ bỏ hoàn toàn các tab không dùng đến trên menu: "Xe Xuất Hàng", "Hàng Giao MQ", "Báo Cáo Xe".
- **Hệ thống Đồng Bộ Online (GitHub):** 
  - Đã thiết lập kho lưu trữ Git và liên kết tới repo `Cofitoes/SupraPhuTho`.
  - Đã khắc phục lỗi xung đột Git (Fast-forward, Pathspec) và đẩy dữ liệu lên Netlify thông qua nhánh `main`.
- **Hệ thống Tự Động Hóa (Automation):**
  - Đã cấu hình Windows Task Scheduler: Tác vụ `Supra_Phu_Tho_AutoUpdate`.
  - Đã tạo chuỗi script: `Cap_Nhat_Tu_Dong_Ngam.vbs` chạy ngầm gọi `Cap_Nhat_Du_Lieu_Auto.bat` (không có lệnh `pause`).
  - Hệ thống hiện tại đang tự động chạy kiểm tra email, xử lý Excel, tính toán Booking và đồng bộ thẳng lên Web/GitHub **mỗi 30 phút một lần**, hoàn toàn vô hình (không chớp màn hình đen).

---
*AI khi đọc được dòng này đã khôi phục thành công toàn bộ ngữ cảnh trước đó. Sẵn sàng nhận lệnh mới từ người dùng!*
